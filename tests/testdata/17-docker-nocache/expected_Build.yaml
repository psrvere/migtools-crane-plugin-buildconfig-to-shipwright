apiVersion: shipwright.io/v1beta1
kind: Build
metadata:
  annotations:
    buildconfig-to-shipwright/conversion-outcome: converted-with-warnings
    buildconfig-to-shipwright/original-triggers: '[{"type":"ConfigChange"}]'
    crane.konveyor.io/conversion-warnings: |-
      [buildconfig-demo/nocache-test-build] Output ImageStreamTag "nocache-test:latest" resolved to fallback URL: image-registry.openshift-image-registry.svc:5000/buildconfig-demo/nocache-test:latest
      [buildconfig-demo/nocache-test-build] No explicit pushSecret found for ImageStreamTag output. Ensure the BuildRun uses a ServiceAccount with internal registry push access.
      [buildconfig-demo/nocache-test-build] BuildConfig nocache-test-build uses runPolicy "Serial", which is dropped: OpenShift queued its builds and ran them one at a time, but Shipwright BuildRuns run concurrently. Serialize the runs in your CI/CD pipeline if build ordering matters, for example when several BuildRuns push the same image tag
      [buildconfig-demo/nocache-test-build] BuildConfig nocache-test-build: ConfigChange trigger is dropped — the automatic first build will not happen; create a BuildRun manually once to start the first build.
      [buildconfig-demo/nocache-test-build] Found 1 trigger(s) (ConfigChange) on BuildConfig nocache-test-build — none work in Shipwright today; builds must be started manually or by your own automation.
    crane.konveyor.io/converted-from: build.openshift.io/v1/BuildConfig/nocache-test-build
  labels:
    app: nocache-test
  name: nocache-test-build
  namespace: buildconfig-demo
spec:
  output:
    image: image-registry.openshift-image-registry.svc:5000/buildconfig-demo/nocache-test:latest
  paramValues:
  - name: no-cache
    value: "true"
  - name: pull
    value: always
  - name: dockerfile
    value: Dockerfile
  source:
    contextDir: .
    git:
      revision: master
      url: https://github.com/openshift/ruby-hello-world.git
    type: Git
  strategy:
    kind: ClusterBuildStrategy
    name: buildah
